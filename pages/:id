<div class="pages_banner">
    <div class="main_container">
        <h2 id="mm_page_title"></h2>
    </div>
</div>
<div class="main_container margin_30">
    <div id="mm_page_content" class="details_desc"></div>
</div>

<script>
    $(document).ready(function(e){
        init(e);
        loadMallDataCached(renderPage);
    })
    
    function renderPage(){
        var pathArray = window.location.pathname.split( '/' );
        var slug = pathArray[pathArray.length-1];
        
        prefix = get_prefix();
        $('#top_banner').addClass(slug)
        var pages_json = prefix+"/pages/" + slug + ".json"
        $.getJSON(pages_json).done(function(data) {
            $('#mm_page_title').html(data.title);
            $('#mm_page_content').html(data.body);
        }).fail(function(jqXHR) {
            if (jqXHR.status == 404) {
                $("#mm_page_content").html("Sorry we can't find the page you're looking for.");
            }
        }); 
        if(slug == "ptc-directions-map" || slug=="ptc-parking"){
            $('.pages_banner').addClass('stores_banner')
            $('.pages_banner').removeClass('pages_banner')
        }
        
        if(slug == "ptc-in-the-community" || slug=="ptc-coming-soon"){
            $('.pages_banner').addClass('events_banner')
            $('.pages_banner').removeClass('pages_banner')
        }
        if(slug == "ptc-leasing-information" || slug == "ptc-contact-us" || slug == "ptc-security" || slug == "ptc-advertising-sponsorship"){
            $('.pages_banner').addClass('about_banner')
        }
        show_content();
    }
</script>